# macOS
.DS_Store
**/.DS_Store

# Local tooling
.claude/

# The Arcade builds, each in its own repo. They live next to the hub build
# they copy from so syncing is a file copy rather than a checkout.
Beat Race Standalone/
Beat Race Demo/
